# Face-Gender-Detection-Using-Deep-Learning-OpenCV
Computer vision project that performs face detection and gender classification on images using OpenCV and a pre-trained deep learning model.  The system detects faces in a group image, crops them, and uses a Caffe-based neural network to predict gender — classifying each face as Male or Female, along with confidence scores.
